import { LibraryCategoryScreen } from "@/features/library/library-category-screen";

/** A normal Library-stack destination that preserves the tab bar and player. */
export default function TabLibraryCategoryScreen() {
    return <LibraryCategoryScreen presentation="tab" />;
}
